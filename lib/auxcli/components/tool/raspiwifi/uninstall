#!/bin/bash
set -e

sudo python3 /usr/lib/raspiwifi/uninstall.py

config="/etc/auxcli/components.json"
tmp="/etc/auxcli/components.json.tmp"

jq '(.[] | select (.name == "raspiwifi") | .installed) = false' $config | sudo tee $tmp
sudo mv -f $tmp $config
