#!/bin/bash
set -e

echo "Uninstalling Pishrink."
sudo rm -rf /usr/local/bin/pishrink.sh

config="/etc/auxcli/components.json"
tmp="/etc/auxcli/components.json.tmp"

jq '(.[] | select (.name == "pishrink") | .installed) = false' $config | sudo tee $tmp 1> /dev/null
sudo mv -f $tmp $config
