#!/bin/bash
set -e

config="/etc/auxcli/components.json"

if jq --arg component "$1" -r '.[] | select( .name == $component ) | .requires_reboot' $config; then
    # echo "$1 requires reboot."
    exit 0
else
    # echo "$1 does not require reboot."
    exit 1
fi
